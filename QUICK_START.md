# 🚀 Быстрый старт с дебагом

## Что-то не работает? Используй эту инструкцию!

### Шаг 1: Открой консоль
1. Нажми **F12** → вкладка **Console**
2. Скопируй/вставь код из файла `TEST_DATA.js` прямо в консоль

Просто скопируй всё содержимое TEST_DATA.js в консоль браузера - это добавит 3 тестовых пользователя.

### Шаг 2: Проверь данные
```javascript
// Увидишь 3 тестовых пользователя
window.debugStorage()
```

### Шаг 3: Зайди как один из них
1. Перезагрузи страницу (F5)
2. Нажми "Вход"
3. Используй: `alice@example.com` / `123456`

### Шаг 4: Проверь друзей
1. Нажми на вкладку **Friends** (👥)
2. Введи `bob` в поиск
3. Должен появиться bob@example.com

### Если всё работает
⭐ Значит проблема в регистрации новых пользователей!
⭐ Нужно проверить Auth.jsx

### Если всё не работает
💔 Есть глубокая проблема с поиском
💔 Нужно дальше дебагить

## Команды для консоли

```javascript
// Добавить тестовых пользователей
window.addTestUsers()

// Просмотреть всё в localStorage
window.debugStorage()

// Открыть debug панель
window.openDebug()

// Очистить всё (WARNING - потеряешь все данные!)
localStorage.clear()

// Проверить текущего пользователя
JSON.parse(localStorage.getItem('qs_user'))

// Проверить всех пользователей
JSON.parse(localStorage.getItem('qs_users'))

// Посмотреть количество пользователей
JSON.parse(localStorage.getItem('qs_users')).length
```

## Логирование в консоли

Когда что-то работает, в консоли должны появиться логи:

**При загрузке страницы:**
```
=== [APP] ИНИЦИАЛИЗАЦИЯ ===
localStorage.getItem("qs_user"): найден
Всего зарегистрировано пользователей: 3
  - alice@example.com
  - bob@example.com
  - charlie@example.com
```

**При открытии Friends:**
```
✓ Friends компонент загружен
=== ПОИСК ПОЛЬЗОВАТЕЛЕЙ ===
localStorage.length: 12
Ищу qs_user_* ключи...
  Найден: qs_user_1001 alice@example.com
  ✓ Добавлен в список
...
```

**При поиске:**
```
🔍 Поиск: bob
Всего пользователей доступно: 2
✓ Совпадение: bob@example.com
✓ Найдено совпадений: 1
```

Если этих логов нет - компонент не загружается!
