# 🔧 Supabase Database Setup

После развертывания на Netlify приложение будет работать в fallback режиме (используя localStorage).

Для полноценной работы нужно создать таблицу в Supabase.

## 📋 Что нужно сделать?

### Шаг 1: Откройте Supabase Console
1. Перейдите на https://app.supabase.com
2. Выберите проект QuitSmoke
3. Перейдите на вкладку "SQL Editor"

### Шаг 2: Создайте таблицу profiles

Скопируйте и запустите этот SQL код:

```sql
-- Создаём таблицу profiles для хранения данных пользователей
CREATE TABLE IF NOT EXISTS public.profiles (
  id uuid NOT NULL PRIMARY KEY REFERENCES auth.users(id) ON DELETE CASCADE,
  email text NOT NULL UNIQUE,
  username text,
  avatar_color text DEFAULT '#667eea',
  avatar text,
  status text,
  bio text,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now()
);

-- Даем доступ всем пользователям к чтению
ALTER TABLE public.profiles ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Public profiles readable" ON public.profiles
  FOR SELECT USING (true);

-- Пользователи могут обновлять только свой профиль
CREATE POLICY "Users can update own profile" ON public.profiles
  FOR UPDATE USING (auth.uid() = id);

-- Пользователи могут вставлять свой профиль при регистрации
CREATE POLICY "Users can insert own profile" ON public.profiles
  FOR INSERT WITH CHECK (auth.uid() = id);
```

### Шаг 3: Запустите SQL

1. Вставьте SQL код в "SQL Editor"
2. Нажмите "Run"
3. Должна появиться зелёная галочка ✅

## ✅ Как правильно работает?

### Без таблицы (сейчас):
- Приложение грузит пользователей из localStorage
- Работает локально в браузере на одном устройстве
- При открытии на другом устройстве - других пользователей не видит

### С таблицей (после SQL):
- При регистрации -> профиль автоматически сохраняется в БД
- Friends вкладка загружает реальных пользователей из таблицы
- Поиск работает со всеми пользователями системы
- Синхронизируется между устройствами

## 🧪 Как проверить что всё работает?

### Вариант 1: Через SQL Editor
```sql
-- Проверяем что таблица создана
SELECT * FROM public.profiles;

-- Должна быть пустая таблица или пользователи если уже кто-то зарегистрировался
```

### Вариант 2: Через приложение
1. Откройте приложение
2. Зарегистрируйтесь
3. Откройте консоль браузера (F12 → Console)
4. Вы должны увидеть:
   ```
   ✓ Профиль добавлен в БД
   ✓ Загружено пользователей: 1
   ```

## 🔐 Security Rules

SQL код выше создаёт:

- **SELECT**: Все могут видеть все профили (публичные данные)
- **UPDATE**: Каждый пользователь может менять только свой профиль
- **INSERT**: При регистрации автоматически создаётся свой профиль

Это безопасно потому что:
- Пользователь не может редактировать других
- Публичные данные (имя, аватар) видны всем
- Приватные данные (если понадобятся) можно защитить отдельно

## ❓ Если что-то не работает?

### Проблема: "Таблица profiles еще не создана"
- Это нормально, просто создайте таблицу через SQL Editor выше

### Проблема: Permission denied
- Проверьте что Row Level Security политики созданы правильно
- Убедитесь что таблица в схеме `public`

### Проблема: Пользователи не сохраняются
- В консоли должна быть ошибка "⚠️ Не удалось создать профиль в БД"
- Проверьте что таблица создана через `SELECT * FROM public.profiles`

## 💡 Если таблица уже существует

Если таблица создана и работает, вы увидите:

```
[Friends] Загружаем пользователей из Supabase...
✓ Загружено пользователей: 3
  - user1@example.com
  - user2@example.com
  - user3@example.com
```

Вместо:

```
⚠️ Таблица profiles еще не создана, используем fallback
✓ Загружено из localStorage: 2
```

---

**После создания таблицы приложение будет в полной рабочей версии!** ✅
