# 🔧 Гайд по Debug-у

## Проблема
- Поиск пользователей не находит никого
- Консоль пуста
- Supabase не отправляет email

## Решение

### Шаг 1: Открой Console
1. Открой приложение в браузере
2. Нажми **F12** или **Ctrl+Shift+I** (Windows/Linux) / **Cmd+Option+I** (Mac)
3. Перейди на вкладку **Console**

### Шаг 2: Проверь сколько пользователей зарегистрировано
В консоли введи:
```javascript
JSON.parse(localStorage.getItem('qs_users')).length
```

Если результат:
- **0** = никто не зарегистрирован (проблема в Auth)
- **1, 2, 3...** = пользователи есть в localStorage ✓

### Шаг 3: Проверь localStorage детально
Введи:
```javascript
window.openDebug()
```

После перезагрузки откроется panic панель со всеми localStorage ключами.

### Шаг 4: Проверь Friends компонент
1. Нажми на вкладку **Friends** (👥)
2. В консоли должны появиться логи:
```
✓ Friends компонент загружен
=== ПОИСК ПОЛЬЗОВАТЕЛЕЙ ===
localStorage.length: ...
Загружено пользователей: ...
```

Если логов нет = Friends компонент не монтируется

### Шаг 5: Протестируй регистрацию
1. Выйди (Logout)
2. Нажми "Регистрация"
3. Введи:
   - Email: `test@example.com`
   - Пароль: `123456`
4. Нажми "Зарегистрироваться"
5. В консоли должно появиться:
```
✓ [AUTH] Пользователь зарегистрирован: test@example.com
Сохранено в qs_users: 1 всего
```

### Шаг 6: Протестируй поиск
1. Зарегистируй ещё одного пользователя:
   - Email: `friend@example.com`
   - Пароль: `123456`
2. Выйди и вернись с первым аккаунтом
3. Нажми на Friends
4. Вводи `friend` в поле поиска
5. В консоли должно появиться:
```
🔍 Поиск: friend
Всего пользователей доступно: 1
✓ Совпадение: friend@example.com
✓ Найдено совпадений: 1
```

## Частые проблемы

### ❌ Консоль совсем пуста после refresh
- Скорее всего дебаг-логирование не добавлено
- Убедись что версия приложения свежая (Netlify перезагружается несколько минут)

### ❌ Нет пользователей в localStorage
- Возможно регистрация вообще не работает
- Проверь ошибки при вводе пароля (минимум 6 символов)
- Проверь что qs_users вообще создается: `localStorage.getItem('qs_users')`

### ❌ Friends компонент не монтируется
- Попробуй перезагрузить страницу
- Проверь что user.id существует в консоли: `console.log(user)`

### ✅ Все работает?
Как только это подтвердишь, мы:
1. Восстановим Supabase интеграцию для email送ing
2. Добавим более надежный поиск
3. Добавим синхронизацию между вкладками

## Команды для quick debug

```javascript
// Все пользователи
JSON.parse(localStorage.getItem('qs_users'))

// Текущий пользователь
JSON.parse(localStorage.getItem('qs_user'))

// Все qs_user_* профили
Object.keys(localStorage).filter(k => k.startsWith('qs_user_')).map(k => [k, JSON.parse(localStorage.getItem(k))])

// Очистить localStorage (WARNING!)
localStorage.clear()

// Открыть debug панель
window.openDebug()

// Показать все логи при регистрации
true // уже сделано
```
